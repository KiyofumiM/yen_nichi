spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/yennichi
    username: yennichi
    password: yennichi

  h2:
    console:
      enabled: true
      
debug: true

image:
  local: true